# 🤖 AI Chat Bot (OpenAI & Anthropic)

![Python](https://img.shields.io/badge/Python-3.10%2B-blue?style=for-the-badge&logo=python)
![OpenAI](https://img.shields.io/badge/OpenAI-API-green?style=for-the-badge&logo=openai)
![Anthropic](https://img.shields.io/badge/Anthropic-Claude-purple?style=for-the-badge&logo=anthropic)
![Status](https://img.shields.io/badge/Status-Active-success?style=for-the-badge)

Удобный консольный чат-бот для работы с современными LLM (Large Language Models) через единый ProxyAPI. Поддерживает модели GPT (OpenAI) и Claude (Anthropic), включая режимы углубленного размышления (Thinking Mode).

---

## ✨ Возможности

*   **Мульти-модельность**: Поддержка широкого спектра моделей:
    *   🔵 **OpenAI**: `gpt-5-mini`, `gpt-5.2`, `o4-mini`, `o3`.
    *   🟣 **Anthropic**: `claude-sonnet-4-5`, `claude-opus-4-5`.
*   **🧠 Thinking Mode**: Полноценная поддержка "думающего" режима для моделей Claude с визуализацией или скрытием процесса размышлений.
*   **🎭 Персоны**: Встроенные стили общения:
    1.  Вежливый ассистент
    2.  Эксперт Python (строгий, технический)
    3.  Токсичный "бузотер" (для веселья)
    4.  5-летний ребенок
*   **🛡️ Надежность**:
    *   Встроенная обработка обрывов соединения и таймаутов.
    *   Корректная работа с лимитами API.
*   **💾 Контекст**: Сохраняет историю диалога в рамках сессии.
*   **⚙️ Гибкая настройка**: Конфигурация через `.env` и интерактивное меню при запуске.

---

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd ai-chat-bot
```

### 2. Создание виртуального окружения
Windows:
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```
Linux/macOS:
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка API ключа
Скопируйте пример файла конфигурации:
```bash
cp .env.example .env
```
Откройте `.env` и вставьте ваш ключ ProxyAPI:
```ini
PROXYAPI_KEY=sk-ВашКлюч...
```

---

## 🎮 Использование

Запустите бота командой:
```bash
python chat_bot.py
```

### Процесс запуска:
1.  **Выбор модели**: Выберите номер модели из списка или введите название вручную.
2.  **Настройка Claude**: Если выбрана модель Anthropic, бот предложит включить *Thinking Mode* и настроить отображение мыслей.
3.  **Выбор персоны**: Выберите желаемый стиль общения.

### Команды в чате:
*   `exit`, `quit`, `выход` — завершить сессию (выведет полную историю переписки).

---

## 📂 Структура проекта

```text
ai-chat-bot/
├── .env                # Ваш API ключ (не пушить в git!)
├── .env.example        # Пример конфигурации
├── .gitignore          # Исключения для git
├── chat_bot.py         # Основной файл приложения (класс AIAssistant)
├── requirements.txt    # Зависимости Python
└── README.md           # Документация
```

## 🛠 Технологии

*   **Python 3**: Основной язык.
*   **OpenAI SDK**: Клиент для GPT и o-серии моделей.
*   **Anthropic SDK**: Клиент для Claude с поддержкой Thinking blocks.
*   **python-dotenv**: Управление переменными окружения.

---
*Developed with ❤️ by AIAssistant*
